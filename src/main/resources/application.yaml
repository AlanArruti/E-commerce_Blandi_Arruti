spring:
  application:
    name: E-commerce

  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${GITHUB_CLIENT_ID}
            client-secret: ${GITHUB_CLIENT_SECRET}
            scope:
              - user:email
              - read:user

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: America/Argentina/Buenos_Aires
    open-in-view: false

server:
  port: 8080

jwt:
  secret: ${JWT_SECRET}
  expiration: 86400000

correo-argentino:
  base-url: https://apitest.correoargentino.com.ar/micorreo/v1
  user-token: ${CORREO_ARGENTINO_USER_TOKEN:}
  password-token: ${CORREO_ARGENTINO_PASSWORD_TOKEN:}
  customer-id: ${CORREO_ARGENTINO_CUSTOMER_ID:}
  codigo-postal-origen: "1425"
